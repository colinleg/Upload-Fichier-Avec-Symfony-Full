<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        <header>
            <div class="h-16 bg-green-300 flex justify-around items-center">
                <a class="text-white text-xl font-bold" href="{{ path('upload') }}">Uploader une image</a>
                <a class="text-white text-xl font-bold" href="{{ path('legumes') }}">Tous les légumes</a>
                <a class="text-white text-xl font-bold" href="/">Home</a>
                
            </div>
        </header>
        {% block stylesheets %}
            <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>
